package net.neoforged.fml.earlydisplay.render;

public record MaterializedThemeSprites(
        Texture progressBarBackground,
        Texture progressBarForeground,
        Texture progressBarIndeterminate
) {
}

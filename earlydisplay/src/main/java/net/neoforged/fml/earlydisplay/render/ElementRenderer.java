/*
 * Copyright (c) NeoForged and contributors
 * SPDX-License-Identifier: LGPL-2.1-only
 */

package net.neoforged.fml.earlydisplay.render;

public abstract class ElementRenderer {
    public void render() {
//
//        @Override
//        public void render(float availableWidth, float availableHeight) {
//            GlState.activeTexture(GL_TEXTURE0);
//            GlState.bindTexture2D(texture.textureId());
//            // TODO ctx.elementShader().updateRenderTypeUniform(ElementShader.RenderType.TEXTURE);
//
//            buffer.begin(SimpleBufferBuilder.Format.POS_TEX_COLOR, SimpleBufferBuilder.Mode.QUADS);
//            var inset = 5f;
//            var x0 = inset;
//            var x1 = inset + texture.width();
//            var y0 = inset;
//            var y1 = inset + texture.height();
//            // TODO int fade = (int) (Math.cos(frame * Math.PI / 16) * 16) + 16;
//            // TODO var colour = (fade & 0xff) << 24 | 0xffffff;
//            // TODO QuadHelper.loadQuad(buffer, x0, x1, y0, y1, 0f, 1f, 0f, 1f, colour);
//
//            buffer.draw();
//        }
    }
}

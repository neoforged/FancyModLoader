plugins {
    id 'java'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

dependencies {
    implementation "com.google.code.gson:gson:2.10.1"
}

repositories {
    gradlePluginPortal()
}
